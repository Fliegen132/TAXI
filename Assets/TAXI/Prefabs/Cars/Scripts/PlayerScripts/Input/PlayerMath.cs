public static class PlayerMath
{
    public static float MathTrunSpeed(float speed) => speed / 600;

    public static float MathRotateSpeed(float speed) => speed / 100;

    public static float MathSpeed(float speed) => speed / 2f;
}
